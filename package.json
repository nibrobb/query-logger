{
  "name": "querylogger",
  "version": "1.0.0",
  "description": "Modular browser query logger with demo integration",
  "main": "dist/esm/index.js",
  "type": "module",
  "scripts": {
    "build": "npm run build:types && npm run build:browser",
    "build:types": "tsc -p tsconfig.json",
    "build:browser": "bun build src/browser.ts --bundle --format iife --target browser --outfile dist/query-logger.js",
    "demo": "npm run build:browser && bun run demo/server.mjs",
    "test": "npm run build:types && bun run test/smoke-test.mjs"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.5.3"
  },
  "private": true
}
